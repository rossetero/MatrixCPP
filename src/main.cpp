#include <iostream>
#include <memory>

#include "s21_matrix_oop.h"

int main() {
  // try {
  //  S21Matrix a(3, 3);

  // // S21Matrix b(2, 3);
  // a.Fill();
  // S21Matrix b(2, 3);

  // // S21Matrix b(2, 3);
  // b.Fill();
  // const S21Matrix ca(a);
  // const S21Matrix cb(b);
  // // const S21Matrix r = ca + cb;

  // // b.Fill();

  // // r.Print();
  // //  a -= r;
  // b.SetCols(4);

  // b.Print();
  // b.SetRows(5);

  // b.Print();
  // double z = -2;
  // a = a * z;
  // a.Print();
  // b.SetCols(3);

  // b.Print();
  // b.SetRows(3);

  // b.Print();
  // a = a * b;
  // a.Print();

  // // a = 2 * r;
  // a.Print();

  // b *= a;
  // b.Print();

  // b *= -1;
  // // a.SumMatrix(b);
  // b.Print();
  // // a.SubMatrix(b);
  // a.Print();

  // a.MulNumber(2);
  // a.Print();

  // a.MulMatrix(b);
  // a.Print();
  // S21Matrix b = std::move(a.Transpose());
  // b.Print();
  // a.Print();

  // a.Transpose().Print();
  // a.InverseMatrix().Print();
  // std::cout << d;
  //   std::cout << a(9, 1) << std::endl;
  //    a = std::move(b);
  //     double& tmp = b(1, 1);

  // const double& z = b(1, 1);
  //  int&& tmp = 400;
  // bool t = a.EqMatrix(b);
  // std::cout << t;
  // } catch (std::bad_alloc& ba) {
  //   std::cout << "BA\n";
  // } catch (std::exception& ex) {
  //   std::cout << ex.what();
  // }

  return 0;
}
